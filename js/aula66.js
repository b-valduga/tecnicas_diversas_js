const mat = document.getElementById('mat');

const num = Math.floor(Math.random() * 10) + 1;
mat.innerHTML = num;

// mat.innerHTML = Math.floor(num); //Math.floor: arredonda

// mat.innerHTML = Math.round(num); //Math.round: arredonda e vai ate o 10

// mat.innerHTML = Math.sqrt(9); //raíz quadrada

// mat.innerHTML = Math.pow(10, 3); //potência

